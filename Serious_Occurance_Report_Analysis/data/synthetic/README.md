# Synthetic source snapshots

Every record is fictional. Three events and four category records include a repeated participant and an event without categories. All events occur in June 2026, so the monthly trend has one populated month.

The independently SQL-loaded equivalent is [the fixture](../../tests/01_synthetic_fixture.sql). SSIS acceptance checks compare complete Silver output against that fixture.
